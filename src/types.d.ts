export interface PresetOptions {
  /**
   * Class prefix for matching Catppuccin colours.
   * Also used for extending `ctp-<colour>` classes by reading from body element's classes
   * @defaultValue `ctp-`
   */
  prefix?: string;
}
